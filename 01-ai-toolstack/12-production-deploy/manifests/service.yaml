apiVersion: v1
kind: Service
metadata:
  name: agent-api
  namespace: agent
spec:
  type: ClusterIP
  selector:
    app: agent-api                    # Blue-Green 时改成 version: green 切流量
  ports:
  - name: http
    port: 80
    targetPort: 8000
