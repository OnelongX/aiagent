# === LLM endpoint (OpenAI 协议兼容)===
# 推荐:livetoken — 一个 base_url 跑 GPT-5/Claude/Gemini/DeepSeek
# 详情见 ../../../../docs/livetoken.md

# PageIndex 用的环境变量(也可以叫 OPENAI_API_KEY)
CHATGPT_API_KEY=sk-xxxxx

# OpenAI 兼容客户端的 base_url(不带 /v1 让代码默认追加)
OPENAI_API_BASE=https://livetoken.top
