services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app/knowledge:/app/app/knowledge
      - ./app/indexes:/app/app/indexes
      - ./data:/app/app/data
    restart: unless-stopped
